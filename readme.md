nodes -> python functions
state -> state schema serves as the input for nodes and edges

graph.add_node("start_play", start_play) 
 -> here "___" is the name and 2nd argument will be fucntion name(node)

 reducers**